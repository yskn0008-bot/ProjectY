# Raw First authority invariant

Physical iPhone evidence showed that model-returned `original_input` may differ from the already-persisted dictation by punctuation, spacing, or normalization. Clarity must never block execution solely because of that mismatch.

The authoritative original is the Raw First record written before the model call. The signed artifact pipeline removes the `original_input_mismatch` execution gate while preserving all other safety gates.
