# ProjectY Codex Rules

## YOSの識別を絶対に維持する

- このプロジェクトの唯一の人格・司令塔の正式名称は **YOS**。
- YOSを別名へ変更しない。
- YOSを別チャット・別人格として作り直さない。
- YOSのピン留め解除を提案・指示しない。
- チャット名やピン留めを変更する実装・手順を作らない。
- アプリ側の表示不具合があっても、現在のYOSチャットを本体として扱う。
- 変更案がこのルールに触れる場合は実行せず、ようすけへ確認する。

このルールは、機能追加・UI変更・自動化・リファクタリングより優先する。

## 役割

- **YOS**：全体判断、優先順位、最終承認。
- **Taxi Lab**：設計、研究、分析、実験。
- **Codex**：コード実装の標準担当。
- **GitHub**：コード、Issue、ブランチ、PR、Actions、実装履歴の正本。

## 開発開始時の必須確認

コードを変更する前に必ず実行する。

1. この `AGENTS.md` と対象フォルダ内のより深い `AGENTS.md` を読む。
2. GitHubの最新 `main`、未マージPR、関連Issue、最近のコミットを確認する。
3. チャット要約だけを信用せず、GitHubから現在地を復元する。
4. 担当範囲と変更予定ファイルを明示する。
5. 完了済み資産を再実装せず、既存資産を改善する。

## Codex優先ルール

コードの作成・編集・修正・リファクタリング・テスト追加は、原則としてCodexで実行する。

例外は次のみ。

- 設計、調査、文章作成、仕様判断だけでコード変更がない。
- Codexが障害、利用制限、接続不良で使用できない。
- YOSが承認した緊急ロールバック。

例外時は、PRの `Codex execution` 欄へ理由を記録する。通常チャットだけで実装を進めない。

## 必須ワークフロー

1. 最新 `main` から専用ブランチを作る。
2. Codexへ対象Issue、担当範囲、完成条件、禁止範囲を渡す。
3. 担当範囲内だけを実装する。
4. 構文確認、関連テスト、回帰テストを実行する。
5. 最終diffで担当外変更と意図しない変更を確認する。
6. PRを作成する。実装を直接 `main` へ入れない。
7. GitHub Actionsとレビュー結果を確認する。
8. 本番確認・iPhone実機確認が残る場合は未完了として記録する。

## 担当範囲

- Taxi開発：`/taxi/`
- Life開発：`/life/`
- YOS / Hero's Journey開発：`/yos/`
- YOSナビ開発：`/nav/`
- QA：原則として不具合、再現手順、影響範囲を報告し、製品コードは変更しない。

共通ファイル、ルート、Service Worker、API、デプロイ設定など、担当外変更が必要になったら実装を止め、対象ファイル・理由・影響をYOSへ報告する。

## 完成の定義

検証できた範囲だけを完了と呼ぶ。

- **コード完成**：実装と自動試験が確認済み。
- **本番完成**：本番デプロイと本番疎通が確認済み。
- **iPhone完成**：対象端末のSafari/PWAで実機確認済み。

実機未確認、本番未確認、外部設定未確認を完成扱いにしない。

## PRへ必ず記録する証拠

- `Codex execution`：使用済み / 利用不可 / 例外。可能ならCodexタスク参照。
- 担当範囲。
- 変更ファイル。
- 実行した試験と結果。
- 担当外変更なしの確認。
- 本番確認状況。
- iPhone実機確認状況。
- 残課題。

## 新規チャット・引き継ぎチャット

新しい開発チャットやフリーズ後の代替チャットは、過去の会話だけから再開しない。最初にGitHubを確認し、次を復元する。

- リポジトリと担当フォルダ。
- 最新mainコミット。
- 関連IssueとPR。
- 検証済みの完了事項。
- 未完了事項。
- 次に実行する1件。

実装が必要なら、確認後にCodexへ委任する。