{
  "name": "yos-ios-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:web": "node ./scripts/prepare-web.mjs",
    "native:init": "npm run prepare:web && npx cap add ios",
    "native:sync": "npm run prepare:web && npx cap sync ios",
    "native:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "typescript": "^5.9.0"
  }
}
