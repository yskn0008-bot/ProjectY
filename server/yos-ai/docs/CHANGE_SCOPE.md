# Change scope

This change touches only `server/yos-ai`.

It does not modify:

- `/taxi/`
- `/yos/`
- `/life/`
- Project75 data
- Google Drive source documents

External production settings remain manual and secret values remain outside GitHub.
